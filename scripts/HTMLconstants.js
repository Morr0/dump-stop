import FileItem from "../components/fileItem.js";

// Holds the markup of the file
const independentFileItem = "";

// param:independent = does not have a child nor a parent
function generateFile(independent, id, title){
    
}